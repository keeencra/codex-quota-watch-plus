# 项目维护约定

## 长期维护

本项目由所有者于 2026-09-12 指定为长期维护项目。后续工作先阅读 [长期维护计划](docs/maintenance.md)，持续记录已确认问题、待办与验证结果。每周检查沿用当前任务；已有自动续签任务独立运行，避免重复安装或并行改动部署。维护应针对实际缺陷与明确需求，不为凑版本新增功能。

## GitHub 同步与更新日志

这是项目所有者要求长期遵守的发布规范：

- 每次同步 GitHub，必须同时维护 `CHANGELOG.md`，明确说明**相比上一次公开版本**新增了什么、修复了什么 Bug。
- 发布分级以 [版本号与发布规则](docs/versioning.md) 为准，采用 `v主版本.次版本.修订号`：仅必要的不兼容升级、核心使用方式实质扩展或整体核心流程重大改版才考虑升主版本；常规兼容功能、局部界面与交互改进升次版本；只修已有 Bug 升修订号。默认从严判断，常规功能混合 Bug 修复仍是小更新，不因新增任意功能就升第一位。主版本升级必须在日志中写出重大变化和分级理由；每位数字可超过 9，不机械进位。
- 仅日志、README、截图／示意图、介绍、发布说明、维护文档或文字勘误，以及不改变产品行为的重构、测试和构建检查，均不单独升级版本。并入所属版本的「文档与维护补充」，不新建版本条目、标签、Release，也不增加迭代次数。随产品变化附带的文档和测试不另计版本。
- 只有有效产品版本发布才累计迭代；一次发布多个提交只计一次。不因项目版本号改变 App 构建号或 Python 包版本。历史 v0.1.0～v2.0.0 保留累计 10 次，此前误标的文档快照不计入有效版本。
- 2026-09-13 所有者明确指定：删除误发后撤回的 v3.0.0 Release 及旧标签，将本次品牌／DeepSeek 产品更新正式编号为 v3.0.0、第 11 次有效迭代；这是针对未生效文档误发的纠正，不允许后续覆盖有效产品版本。
- 产品发布时同步 VERSION、README 版本／类型／迭代、CHANGELOG 与 docs/downloads.md，并提供固定源码和 Release。文档补充只修改现有说明；不得移动已有标签或重写原始下载包，用独立链接区分当前文档与发布源码。
- 已有历史阶段已追溯编号，已生效的产品版本不重排、复用或删除；日期和旧更新序号继续保留。未创建的 Git 标签或 GitHub Release 不得宣称已发布。
- 每次产品发布独立成条；注明更新类型、分级理由、上一版提交或比较链接。同一批相关改动统一发布，不因每个提交单独发版本；文档补充记在所属版本下并注明日期，不占用产品更新序号，不把不同发布全部混成一个日期条目。
- 固定包含「新增内容」「Bug 修复」「改进」「验证与升级说明」；没有新增或修复时明确写「无」，不得编造变化。
- Bug 写清楚触发条件、之前的错误表现及修复后的表现。区分新增功能、文档完善和已有功能展示，不能把文档新增写成 App 新增能力。
- 记录实际执行的验证及其限制。不得把模拟器展示、编译通过或推送服务接收等同于真机端到端验证。
- 比较已发布的 `origin/main` 与拟发布内容；发布后核对远端文件内容。个人 Token、签名、真实审批详情、设备日志不可上传。

## 项目介绍与图片

- README 介绍本项目当前实际能力，优先展示手机、手表、任务、审批、额度和提醒的用户流程。
- 每次产品同步逐项核对 README 的开头定位、主要功能正文、功能表、安装入口和展示图；不能只更新版本号或一句最新版本摘要。新界面采用已经验证的最新项目图片，未改动的平台保留原图并注明对应版本；历史布局不得作为当前功能介绍。无界面变化的 Bug 修复要同步相关行为说明。
- 推送后打开 GitHub 项目首页，确认主要标题、功能内容、图片加载与下载链接实际生效，再报告首页已更新。仅介绍修订并入当前版本文档补充，不另发产品版本。
- 图片必须来自本项目；说明是实机、模拟器、演示数据还是示意图。禁止把借鉴项目截图或未实现的设计稿当成当前产品。
- 截图不包含真实任务内容、配对二维码、密钥或私人设备信息。
- 保留许可证及必要的上游著作权、致谢，不将借鉴项目的文案作为本项目主体介绍。

## 更新日志配图

- 有可见界面或交互改动时，在对应版本、对应新增或修复说明旁加入 1～3 张图片演示；优先同尺寸、同入口的真实前后截图，配一句「之前 → 之后」说明。没有可见变化时写明即可，不为配图编造界面。
- 图片注明改动版本、实机／模拟器／演示数据／示意图；没有旧截图时可以用明确标注的行为示意图，不冒充真实截图。保留文字说明和准确的替代文字。
- 配图放在 `docs/assets/`，文件名包含改动版本，保留可编辑源文件。已发布图片不覆盖；补充旧版本图片时注明补图日期，不把旧修复写成新功能。
- GitHub Release 同步展示主要配图，使用固定提交的绝对图片地址（补图提交可晚于产品标签）；发布前验证图片可读、链接有效、无私人信息。


## Mac 显示模块

`macos/` 是码伴可选的菜单栏与 WidgetKit 显示端，按根目录版本与日志规则维护；不使用来源项目的独立版本计数。修改时运行 `bash macos/scripts/check.sh`，保留 MIT 声明、现有签名标识与共享数据。原独立工程有其他未提交改动时不得覆盖。安装显示端不等于已部署手机服务，不宣称四端实时共用同一次采样。

## 跨端组件布局

`shared-widgets/ClassicQuotaView.swift` 由 Mac 与 iPhone 共用，账户总览复用 `macos/Sources/Widget/QuotaWidget.swift`。修改共用视图时验证两端构建，保留旧 kind、App Group 与无 DeepSeek 布局；展示图注明离屏渲染及虚构数据。Mac 近期任务用量与手机今日用量不混称同一指标。
