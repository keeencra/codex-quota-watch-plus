# 项目维护约定

## 长期维护

本项目由所有者于 2026-09-12 指定为长期维护项目。后续工作先阅读 [长期维护计划](docs/maintenance.md)，持续记录已确认问题、待办与验证结果。每周检查沿用当前任务；已有自动续签任务独立运行，避免重复安装或并行改动部署。维护应针对实际缺陷与明确需求，不为凑版本新增功能。

## GitHub 同步与更新日志

这是项目所有者要求长期遵守的发布规范：

- 每次同步 GitHub，必须同时维护 `CHANGELOG.md`，明确说明**相比上一次公开版本**新增了什么、修复了什么 Bug。
- 每次公开更新必须分为「大更新」或「小更新」，并同步 `VERSION`、README 顶部版本／类型／迭代次数、CHANGELOG 版本索引及新条目。按项目所有者约定采用 `v主版本.0.修订号`：新增功能、界面调整、行为改进或新增／重写介绍、截图、文档等内容为大更新，主版本 +1、修订号清零（如 v1.0.2 → v2.0.0）；仅修复已有 Bug 且无新增内容为小更新，仅修订号 +1（如 v1.0.0 → v1.0.1）。修复说明及必要回归测试随 Bug 发布仍算小更新；单纯勘误按修正归类。混合发布按大更新计算。中间位固定为 0，不把“大更新”等同于不兼容变更；这是一套项目自定义版本约定。
- 迭代次数单独维护：每次公开发布 +1，一次发布的多个 Git 提交只计一次；不可再从版本号推算累计次数。除非项目所有者另行指定，不因项目版本变化而改动 App 构建号或 Python 包版本。
- 每次发布同步 `docs/downloads.md`，为新版本提供固定下载入口及 GitHub Release；版本标签必须指向已核对的发布提交，发布后验证 ZIP 下载可用。历史下载链接固定到原提交或标签，不得改成 main。
- 已有历史阶段已追溯编号，不重排、复用或删除旧版本号；日期和旧更新序号继续保留。未创建的 Git 标签或 GitHub Release 不得宣称已发布。
- 每次更新独立成条，同一天多次更新用序号区分；写明上一版提交或比较链接，不把不同发布全部混成一个日期条目。
- 固定包含「新增内容」「Bug 修复」「改进」「验证与升级说明」；没有新增或修复时明确写「无」，不得编造变化。
- Bug 写清楚触发条件、之前的错误表现及修复后的表现。区分新增功能、文档完善和已有功能展示，不能把文档新增写成 App 新增能力。
- 记录实际执行的验证及其限制。不得把模拟器展示、编译通过或推送服务接收等同于真机端到端验证。
- 比较已发布的 `origin/main` 与拟发布内容；发布后核对远端文件内容。个人 Token、签名、真实审批详情、设备日志不可上传。

## 项目介绍与图片

- README 介绍本项目当前实际能力，优先展示手机、手表、任务、审批、额度和提醒的用户流程。
- 图片必须来自本项目；说明是实机、模拟器、演示数据还是示意图。禁止把借鉴项目截图或未实现的设计稿当成当前产品。
- 截图不包含真实任务内容、配对二维码、密钥或私人设备信息。
- 保留许可证及必要的上游著作权、致谢，不将借鉴项目的文案作为本项目主体介绍。

## 更新日志配图

- 有可见界面或交互改动时，在对应版本、对应新增或修复说明旁加入 1～3 张图片演示；优先同尺寸、同入口的真实前后截图，配一句「之前 → 之后」说明。没有可见变化时写明即可，不为配图编造界面。
- 图片注明改动版本、实机／模拟器／演示数据／示意图；没有旧截图时可以用明确标注的行为示意图，不冒充真实截图。保留文字说明和准确的替代文字。
- 配图放在 `docs/assets/`，文件名包含改动版本，保留可编辑源文件。已发布图片不覆盖；补充旧版本图片时注明补图版本，不把旧修复写成新功能。
- GitHub Release 同步展示主要配图，使用固定版本的绝对图片地址；发布前验证图片可读、链接有效、无私人信息。
