# 长期维护计划

生效日期：2026-09-12。项目：码伴 · CodeCompanion（原 Codex Quota Watch Plus）。

## 维护范围

- `macos/` 菜单栏与桌面 WidgetKit 显示、共享容器、码伴凭据复用；Mac 相关改动运行 `bash macos/scripts/check.sh`，与手机服务分别验收。

- Mac 服务与 HTTPS 外网连接、iPhone / Apple Watch 同步和 iPhone 小组件数据一致性。
- Codex 项目分组、任务标题、任务状态和带项目／任务名称的完成通知。
- 手表远程审批：请求来源、有效期、逐项批准／拒绝及结果回传；不得绕过 Codex 自身审批。
- Plus / Pro 与实际额度窗口显示、系统兼容性、现有自动续签。
- GitHub 介绍、真实界面展示、版本日志、固定下载渠道和上游许可证。

## 维护节奏

每周一北京时间 10:00 在现有任务检查一次；用户反馈随时处理。先比较上次本地维护记录、工作区和 GitHub 状态，再检查变化涉及的服务、测试、问题及发布入口。正常或同一非紧急问题未变化时保持安静；发现重要故障、完成修复或需要账号／设备操作时才通知。已有自动续签任务继续负责签名有效期与安装，不重复安排。

周检与自动续签共用同一个自动任务：每天北京时间 10:00、20:00 检查签名，仅周一 10:00 额外执行周检，不重复安装。

## 修复与发布

1. 先复现并记录触发条件、影响范围和预期行为，再做最小必要修复。不得覆盖用户未提交修改。
2. 根据改动运行 Python / Swift 检查；界面改动增加 Xcode 构建。不能把编译、模拟器或推送服务器接收当作真机验证。
3. 涉及真机时保留配对和应用数据。账号确认、设备信任或设备未连接时明确所需操作，不重置应用或设备。
4. 界面改动按 [配图规范](../AGENTS.md#更新日志配图)增加前后截图或标注清楚的示意图。按 AGENTS.md 同步 GitHub、CHANGELOG、版本索引、下载和 Release；无变化不发版本。按 [版本规则](versioning.md) 从严分级：重大升级才升第一位，常规功能与局部改进升第二位，Bug 修复升第三位。日志、配图等文档补充不升版本、不增加迭代，更新所属版本的说明即可。
5. 发布前检查公开仓库差异和敏感信息；不得上传运行配置、签名、密钥、设备标识、真实任务和审批详情。私有部署标识不得被公开示例替换。
6. 完成后记录实际验证、未验证部分、对应版本及后续行动。新增功能先依据用户需求形成具体方案，不自动扩大范围。

## 当前版本与验收

- 当前版本 v3.0.1，第 12 次有效迭代；本次修复完整项目列表和排序同步，保留 v3.0.0 品牌／DeepSeek 功能。
- 152 项 Python、72 项共享 Swift 测试与 Xcode 构建通过；用户已确认 DeepSeek 在手机、手表、小组件显示。最终品牌更新的手机和手表安装回执已确认，视觉效果待用户查看。
- 图库使用隔离模拟器和虚构数据，覆盖主要页面及 Plus／Pro、DeepSeek 配置差异。保留原仓库地址、签名标识、配对和数据；私有续签工程已更新。
- 后续周期核查结果保存在自动化本地维护记录中，不把私有运行状态写入公开仓库。


## 2026-09-13 本地维护：完整项目列表

- 所有者确认当前已有项目顺序正确，缺少的是无本地任务的项目；手机与手表「全部任务」现在同步完整 Mac 项目清单，保留空项目提示。
- 同时处理已迁移数据库与旧全局配置冲突时的排序优先级；置顶及未分组逻辑保留，每次请求读取新顺序。
- 152 项 Python、72 项共享 Swift 测试及手机／手表签名构建通过；本地已部署 Mac 服务，接口返回完整有序项目清单，缓存策略为 no-store。
- 手机与手表安装回执均为 success；设备上的最终显示待所有者查看，不把安装成功当作视觉验收。
- 作为 v3.0.1 Bug 修复发布，第 12 次有效迭代；同步版本索引、图示、下载和保守版本号规则，保留全部历史有效版本。

## 2026-09-13 本地维护：Mac 显示端整合

- 新增可选 `macos/` 模块，整合当前独立工程并统一码伴品牌，保留原工程未提交内容及 MIT 声明；按兼容扩展准备 v3.1.0，尚未发布。
- 原有额度解析与 Widget 摘要检查通过，新增 5 种凭据查找场景通过。签名构建及安装后宿主／扩展共享容器检查通过；进程运行，共享摘要持续更新。
- 云同步工作目录附加属性曾导致安装前签名检查失败，旧应用未被替换；改用本地缓存构建后安装成功，旧应用和启动配置均已备份。
- 原生视图以虚构数据离屏渲染并检查；菜单栏界面操作工具连接超时，实际桌面交互与各尺寸视觉效果待确认。手机／手表源码和部署未变更，无需重装。

### Mac 大号组件布局调整（同批 v3.1.0）

- 按所有者要求将重置卡放到 Codex 额度右侧，DeepSeek 余额移到下方，CNY／USD 并排；小号／中号沿用既有布局。
- 渲染检查 Pro、Plus、无 DeepSeek 配置及多张卡场景，未见遮挡；更新日志补充同一 Plus 场景的前后对比图。图片为虚构数据。
- Mac 全套检查及签名构建通过；宿主／扩展构建号 21 安装成功，共享容器检查通过，菜单栏进程运行。实际桌面重绘时间由系统决定，渲染验收不等于实机桌面验收。

- 桌面旧布局反馈核查：安装包为构建 21，但扩展进程早于升级启动；修正安装器重新注册扩展并退出旧进程，同时去掉重复启动宿主的操作。重新安装后确认单个宿主、新扩展进程及保留的两个系统组件配置；屏幕最终显示待用户确认。

### 两端统一组件分类

- 额度速览（小／中）与账户总览（大）同时提供 Mac／iPhone 原生配置；速览视图集中在 `shared-widgets/`，大号共用现有视图。保留既有 kind 与 App Group。
- API 增加允许公开给已配对客户端的重置卡摘要与绝对重置时间，Mac 已部署；实际鉴权查询确认新字段有数据。没有卡片标识、兑换能力或新鉴权入口。
- Python 全套 156 项及追加重置卡专项 5 项、Swift 74 项通过；两端签名构建和安装通过。用户已确认 iPhone 可见两个入口；Mac 目录待实机确认。批量离屏渲染曾出现字体丢失，改为每种经典布局独立进程渲染后检查通过，不将此列为产品界面 Bug。
- Mac 中号的「近期任务 token」与手机「今日 token」保留准确口径，不伪造统一采样。手机无需配对手表也能添加组件。

### 统一小组件视觉（同批 v3.1.0）

- 两端共用 `shared-widgets/WidgetAppearance.swift` 调色定义，速览和总览统一黑底、绿色分段额度条、蓝色 DeepSeek 与紫色重置卡；账户总览增加右上刷新按钮，保留布局分类和现有 kind。
- Pro／Plus、大／中／小、无 DeepSeek 等原生离屏演示已检查。Mac 检查和两端签名构建通过；本机安装成功并重载旧扩展。图片均为虚构数据，实际系统着色模式以设备设置为准。

### 单色状态与排版修正（同批 v3.1.0）

- 速览移除内容层的黑色不透明视图，背景只由 WidgetKit 容器绘制；进度条轨道透明度 0.16，已填充 1，去色后保留差别。不修改用户系统外观设置。参考 Apple 的 accented rendering 指南。
- 大号按实际内容分配上区高度并放大数据；统计修复 Codex 缓存输入重复相加及不变累计事件重复计入。保留通用 usage 格式及旧快照兼容。
- Python 159 项、Swift 74 项、Mac 检查与两端签名构建通过。Mac／iPhone 已安装，运行服务已部署统计修正；实际窗口切换待用户确认。离屏截图文字丢失另通过 AppKit 承载 SwiftUI 渲染处理，不作为用户组件问题的原因。

## 2026-09-13 中号布局收敛（待发布 v3.1.0）

- Mac 历史账户总览中号与额度速览、iPhone 中号共用布局；保留旧 kind。余额为主要信息，用量和卡数降为底部辅助文字，无 DeepSeek 时右侧显示卡数。
- Mac 适配层补齐只读重置卡字段；保留 Mac 近期任务／手机今日用量口径差异。
- Mac 检查、签名构建与 iPhone Xcode 构建通过，两端安装成功。离屏检查 Plus、Pro、有／无 DeepSeek 排版；桌面实际视觉效果待用户确认。未推送 GitHub，未新增产品版本号。

## 2026-09-13 组件入口与套餐兼容修复

- 上次仅融合中号布局，没有融合组件注册。本次两端仅注册一个组件，提供小中大尺寸；Mac 保留 CodexUsageWidget，iPhone 保留 CodingQuotaWidget。旧 Mac 速览／旧 iPhone 总览需重新添加。
- 本机摘要实测套餐字符串为 Pro Lite，原共享解析器只识别 prolite。修复空格、下划线和连字符变体，补充回归断言；74 项 Swift 测试通过。
- Mac 检查和两端签名构建通过；安装结果在本地核对。未推送 GitHub，仍并入待发布 v3.1.0。

## 2026-09-13 本地维护：大号组件排版

- 上半区垂直居中，缩短竖线；放大时间及主数字，新增小时级重置／到期提示，固定 CNY 在左、USD 在右。
- Mac 检查和签名构建、iOS 模拟器构建通过；原生离屏检查覆盖 Pro、Plus、多卡、无 DeepSeek。Mac 构建 28 已安装，实机最终显示待用户确认；手机签名构建及安装通过，最终显示待用户确认。
- 与菜单栏双币种修复合并发布次版本 v3.2.0，第 14 次迭代。
